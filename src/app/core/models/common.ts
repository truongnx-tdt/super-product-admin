export enum StorageKey {
    AT = "accessToken",
    RT = "rfToken",
}

export enum Route {
    SIGN_IN = 'auth/sign-in',
    HOME = 'home',
    NOT_FOUND = 'not-found',
    SERVER_ERROR = 'server-error',
    EMPTY = '',
    ADMIN = 'admin',
}